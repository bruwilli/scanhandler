<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Live Scan added for <%= "#{@person.first_name} #{@person.last_name}" %></h1>
    <h2>Cal South ID: <%= @person.cal_south_id || 'Unknown'%></h2>
    <p>
      A new live scan was just added for <%= link_to "#{@person.first_name} #{@person.last_name}", person_url(@person) %>
    </p>
    <h3>Scan information</h3>
    <strong>Scan Date: </strong><%= @scan.scan_date %><br>
    <strong>Added By: </strong><%= @user.name %><br>
    <strong>Date Added: </strong><%= @scan.created_at.to_date %>
  </body>
</html>